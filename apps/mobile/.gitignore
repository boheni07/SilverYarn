# Android/Gradle 표준 무시 목록 + CONVENTIONS.md §4.1(local.properties는 시크릿 취급, Git 제외)

*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build/
/captures/
.externalNativeBuild/
.cxx/
local.properties
/app/build/

# Gradle wrapper jar — 이 스캐폴딩 시점 환경에는 Gradle이 없어 생성 못 함
# (README.md "환경 제약" 참조). 실 Android 환경에서 `gradle wrapper`로 생성한 뒤
# 이 규칙을 지우고 wrapper 산출물을 커밋할 것(표준 Android 프로젝트 관례).
gradle/wrapper/gradle-wrapper.jar
